[pytest]
testpaths = tests
# Los tests importan `tests.conftest`: la raíz tiene que estar en el path
# también cuando se corre `pytest` pelado (CI) y no `python -m pytest`.
pythonpath = .
asyncio_mode = auto
filterwarnings =
    error::DeprecationWarning:copilot.*
